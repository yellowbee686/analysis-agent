[project]
name = "local-file-agent"
version = "0.1.0"
description = "Local file analysis agent with CAMEL and Streamlit"
readme = "DEV_DOC.md"
requires-python = ">=3.11"
dependencies = [
  "camel-ai",
  "pyyaml>=6.0.1",
  "python-dotenv>=1.0.0",
  "rank-bm25>=0.2.2",
  "streamlit>=1.38.0",
]

[tool.uv]
dev-dependencies = [
  "pytest>=8.0.0",
  "ruff>=0.6.0",
]
